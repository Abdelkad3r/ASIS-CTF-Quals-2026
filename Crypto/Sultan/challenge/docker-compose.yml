version: '3.8'

services:
  sultan-challenge:
    build: .
    container_name: sultan_local_test
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLAG=ASIS{fake_flag_for_local_testing}
      - SECRET_KEY=local_test_secret_key
